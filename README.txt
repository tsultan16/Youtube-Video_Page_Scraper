#A simple Youtube video page scraper using Puppeteer.

Scraped data is stored in a JSON file.

Note: Use puppeteer version: 18.1.0 instead of latest version, to avoid issues with not being able to find Chromium during app deploymeny
